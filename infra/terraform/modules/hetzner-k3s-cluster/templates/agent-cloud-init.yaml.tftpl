#cloud-config
package_update: true
packages:
  - curl
  - ca-certificates
runcmd:
  - |
    curl -sfL https://get.k3s.io | \
      K3S_URL="https://${control_private_ip}:6443" \
      K3S_TOKEN="${k3s_token}" \
      INSTALL_K3S_CHANNEL=stable \
      sh -s - agent ${extra_node_args}
