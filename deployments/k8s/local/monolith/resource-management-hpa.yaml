apiVersion: v1
kind: ResourceQuota
metadata:
  name: mono-resource-quota
  namespace: mono
spec:
  hard:
    requests.cpu: "15800m"
    requests.memory: "27648Mi"
    limits.cpu: "15800m"
    limits.memory: "27648Mi"
# NOTE: Monolith HPA is not part of the active benchmark design.
# The monolith remains on its fixed single-pod baseline for all runs.
# This file is kept for structural parity with the MSA local HPA manifest
# but does not deploy any HPA object.
